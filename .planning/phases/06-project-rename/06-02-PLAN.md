---
phase: 06-project-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/aliast.plugin.zsh
  - docs/terminal-compatibility.md
  - CLAUDE.md
autonomous: true
requirements: [REN-03, REN-05, REN-06]

must_haves:
  truths:
    - "Plugin file is named aliast.plugin.zsh"
    - "All zsh functions are named _aliast_* (not _alias_*)"
    - "All zsh state variables are named _ALIAST_* (not _ALIAS_*)"
    - "All user-facing env vars reference ALIAST_* (not ALIAS_*)"
    - "Socket path in plugin uses aliast-$UID/aliast/aliast.sock"
    - "memo=aliast tag used in region_highlight (not memo=alias)"
    - "Temp file prefix uses aliast-nl (not alias-nl)"
    - "Daemon spawn command uses aliast-daemon (not alias-daemon)"
    - "Documentation references updated to aliast"
  artifacts:
    - path: "plugin/aliast.plugin.zsh"
      provides: "Renamed zsh plugin"
      min_lines: 400
    - path: "docs/terminal-compatibility.md"
      provides: "Updated docs"
      contains: "aliast"
  key_links:
    - from: "plugin/aliast.plugin.zsh (_ALIAST_SOCKET_PATH)"
      to: "crates/aliast-daemon/src/lifecycle.rs (default_socket_path)"
      via: "Socket path must match between plugin and daemon"
      pattern: "aliast-\\$UID.*/aliast/aliast\\.sock"
    - from: "plugin/aliast.plugin.zsh (ALIAST_SUGGESTION_STYLE)"
      to: "env var name"
      via: "User sets env var, plugin reads it"
      pattern: "ALIAST_SUGGESTION_STYLE"
    - from: "plugin/aliast.plugin.zsh (command aliast-daemon start)"
      to: "binary name from Cargo.toml"
      via: "Plugin spawns daemon binary by name"
      pattern: "aliast-daemon start"
---

<objective>
Rename the zsh plugin file and replace all 134 internal identifiers from alias to aliast, plus update documentation files.

Purpose: Completes the user-facing rename (REN-03, REN-05) and ensures the plugin socket path matches the daemon's new path (REN-06).
Output: Renamed plugin file with all identifiers updated, docs updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-project-rename/06-CONTEXT.md
@.planning/phases/06-project-rename/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename plugin file and replace all identifiers</name>
  <read_first>
    - plugin/alias.plugin.zsh
    - .planning/phases/06-project-rename/06-RESEARCH.md (Zsh plugin section with all 134 occurrences categorized)
  </read_first>
  <files>plugin/aliast.plugin.zsh</files>
  <action>
    Rename the plugin file and replace every occurrence of old naming with new naming. The replacements are categorized below.

    **Step 1: Rename the file**
    - `mv plugin/alias.plugin.zsh plugin/aliast.plugin.zsh`

    **Step 2: Apply all replacements in the file content**

    The following replacements must be applied. Order matters -- replace longer strings first to avoid partial matches.

    **File header (line 1):**
    - `# alias.plugin.zsh` -> `# aliast.plugin.zsh`
    - `alias-daemon` -> `aliast-daemon` (in header comment)

    **Guard and module loading (lines 6-12):**
    - `_ALIAS_LOADED` -> `_ALIAST_LOADED` (2 occurrences: check + set)
    - `"alias: zsh/net/socket` -> `"aliast: zsh/net/socket`

    **State variables (lines 16-25) -- 12 variables, each `_ALIAS_` -> `_ALIAST_`:**
    - `_ALIAS_FD` -> `_ALIAST_FD`
    - `_ALIAS_REQ_ID` -> `_ALIAST_REQ_ID`
    - `_ALIAS_HIGHLIGHT_ENTRY` -> `_ALIAST_HIGHLIGHT_ENTRY`
    - `_ALIAS_LAST_BUFFER` -> `_ALIAST_LAST_BUFFER`
    - `_ALIAS_SOCKET_PATH` -> `_ALIAST_SOCKET_PATH`
    - `_ALIAS_NL_STATE` -> `_ALIAST_NL_STATE`
    - `_ALIAS_NL_PROMPT` -> `_ALIAST_NL_PROMPT`
    - `_ALIAS_LAST_EXIT` -> `_ALIAST_LAST_EXIT`
    - `_ALIAS_GIT_BRANCH` -> `_ALIAST_GIT_BRANCH`
    - `_ALIAS_LAST_HISTNUM` -> `_ALIAST_LAST_HISTNUM`
    - `_ALIAS_LAST_RECORDED` -> `_ALIAST_LAST_RECORDED`

    **Socket path default value (line 21):**
    - `alias-$UID}/alias/alias.sock` -> `aliast-$UID}/aliast/aliast.sock`
    - Full line: `typeset -g _ALIAST_SOCKET_PATH="${XDG_RUNTIME_DIR:-/tmp/aliast-$UID}/aliast/aliast.sock"`

    **Function names (lines 28-463) -- 18 functions, each `_alias_` -> `_aliast_`:**
    - `_alias_connect` -> `_aliast_connect`
    - `_alias_disconnect` -> `_aliast_disconnect`
    - `_alias_reconnect` -> `_aliast_reconnect`
    - `_alias_resolve_style` -> `_aliast_resolve_style`
    - `_alias_show_ghost` -> `_aliast_show_ghost`
    - `_alias_clear_ghost` -> `_aliast_clear_ghost`
    - `_alias_request_suggestion` -> `_aliast_request_suggestion`
    - `_alias_self_insert_wrapper` -> `_aliast_self_insert_wrapper`
    - `_alias_nl_aware_accept` -> `_aliast_nl_aware_accept`
    - `_alias_accept_suggestion` -> `_aliast_accept_suggestion`
    - `_alias_accept_word` -> `_aliast_accept_word`
    - `_alias_line_pre_redraw` -> `_aliast_line_pre_redraw`
    - `_alias_nl_deactivate` -> `_aliast_nl_deactivate`
    - `_alias_nl_generate` -> `_aliast_nl_generate`
    - `_alias_nl_toggle` -> `_aliast_nl_toggle`
    - `_alias_nl_escape` -> `_aliast_nl_escape`
    - `_alias_precmd_record` -> `_aliast_precmd_record`
    - `_alias_nl_precmd` -> `_aliast_nl_precmd`
    - `_alias_nl_line_init` -> `_aliast_nl_line_init`

    **User-facing env vars (per D-01: hard cut):**
    - `ALIAS_SUGGESTION_HIGHLIGHT` -> `ALIAST_SUGGESTION_HIGHLIGHT` (lines 54, 56)
    - `ALIAS_SUGGESTION_STYLE` -> `ALIAST_SUGGESTION_STYLE` (line 61)

    **Binary name in reconnect (line 46):**
    - `command alias-daemon start` -> `command aliast-daemon start`

    **memo tags in region_highlight (CRITICAL - Pitfall 3 from research):**
    - `memo=alias` -> `memo=aliast` (3 occurrences: line 80 in show_ghost, line 86 comment, line 87 filter pattern)

    **Temp file prefix (Pitfall 5 from research, line 244):**
    - `alias-nl.XXXXXX` -> `aliast-nl.XXXXXX`

    **Compatibility warning (line 467):**
    - `print "alias: zsh-autosuggestions` -> `print "aliast: zsh-autosuggestions`

    **Widget registrations (must match new function names):**
    - `zle -N self-insert _alias_self_insert_wrapper` -> `zle -N self-insert _aliast_self_insert_wrapper`
    - `zle -N accept-line _alias_nl_aware_accept` -> `zle -N accept-line _aliast_nl_aware_accept`
    - `zle -N _alias_accept_suggestion` -> `zle -N _aliast_accept_suggestion`
    - `bindkey '^I' _alias_accept_suggestion` -> `bindkey '^I' _aliast_accept_suggestion`
    - `zle -N _alias_accept_word` -> `zle -N _aliast_accept_word`
    - `bindkey '^[[Z' _alias_accept_word` -> `bindkey '^[[Z' _aliast_accept_word`
    - `bindkey '\e[Z' _alias_accept_word` -> `bindkey '\e[Z' _aliast_accept_word`
    - `add-zle-hook-widget zle-line-pre-redraw _alias_line_pre_redraw` -> `... _aliast_line_pre_redraw`
    - `zle -N _alias_nl_toggle` -> `zle -N _aliast_nl_toggle`
    - `bindkey '^ ' _alias_nl_toggle` -> `bindkey '^ ' _aliast_nl_toggle`
    - `zle -N _alias_nl_escape` -> `zle -N _aliast_nl_escape`
    - `bindkey '\e' _alias_nl_escape` -> `bindkey '\e' _aliast_nl_escape`
    - `add-zsh-hook precmd _alias_precmd_record` -> `... _aliast_precmd_record`
    - `add-zsh-hook precmd _alias_nl_precmd` -> `... _aliast_nl_precmd`
    - `add-zle-hook-widget zle-line-init _alias_nl_line_init` -> `... _aliast_nl_line_init`

    **Implementation approach:** Read the entire file, apply ALL the replacements above systematically, write the new file. The safest approach is to do a complete rewrite of the file with all substitutions applied at once, since every line has at most one type of substitution.

    **Verification after writing:** Run `grep -c "_alias_\|_ALIAS_\|ALIAS_\|memo=alias\|alias-daemon\|alias-nl\|alias\.plugin\|alias\.sock\|/alias/" plugin/aliast.plugin.zsh` -- must return 0.
  </action>
  <verify>
    <automated>cd /Users/chef/Desktop/projects/Alias && test -f plugin/aliast.plugin.zsh && ! test -f plugin/alias.plugin.zsh && echo "FILE_RENAMED_OK" && grep -c "_alias_\|_ALIAS_[A-Z]\|[^T]ALIAS_\|memo=alias[^t]\|alias-daemon\|alias-nl\.\|alias\.plugin\|alias\.sock" plugin/aliast.plugin.zsh | grep -q "^0$" && echo "CONTENT_CLEAN" || echo "STALE_REFS_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f plugin/aliast.plugin.zsh` succeeds (file exists at new name)
    - `test ! -f plugin/alias.plugin.zsh` succeeds (old file gone)
    - `grep -c "_alias_" plugin/aliast.plugin.zsh` returns 0 (no old function names)
    - `grep -c "_ALIAS_" plugin/aliast.plugin.zsh` returns 0 (no old state vars -- note: `_ALIAST_` contains `_ALIAS` but grep for exact `_ALIAS_` with trailing underscore distinguishes)
    - `grep -c "ALIAS_SUGGESTION" plugin/aliast.plugin.zsh` returns 0 (only ALIAST_SUGGESTION)
    - `grep -c "memo=alias[^t]" plugin/aliast.plugin.zsh` returns 0 (memo=aliast only)
    - `grep -c "alias-daemon" plugin/aliast.plugin.zsh` returns 0 (only aliast-daemon)
    - `grep -c "alias-nl" plugin/aliast.plugin.zsh` returns 0 (only aliast-nl)
    - `grep -c "alias\.sock" plugin/aliast.plugin.zsh` returns 0 (only aliast.sock)
    - `grep "aliast-daemon start" plugin/aliast.plugin.zsh` finds the reconnect line
    - `grep "memo=aliast" plugin/aliast.plugin.zsh` finds the region_highlight entries
    - `grep "_ALIAST_SOCKET_PATH" plugin/aliast.plugin.zsh` finds the socket path variable
    - `grep "aliast.sock" plugin/aliast.plugin.zsh` finds the socket filename
    - File has 460+ lines (same as original, no content lost)
  </acceptance_criteria>
  <done>
    Plugin file renamed to aliast.plugin.zsh with all 134 internal identifiers updated: function names use _aliast_*, state vars use _ALIAST_*, env vars use ALIAST_*, socket path uses aliast-$UID/aliast/aliast.sock, memo tags use memo=aliast, temp files use aliast-nl prefix, daemon binary reference is aliast-daemon.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update documentation files and rename GitHub repository</name>
  <read_first>
    - docs/terminal-compatibility.md
    - CLAUDE.md
  </read_first>
  <files>
    docs/terminal-compatibility.md,
    CLAUDE.md
  </files>
  <action>
    **Update docs/terminal-compatibility.md:**
    - Line 39: `source plugin/alias.plugin.zsh` -> `source plugin/aliast.plugin.zsh`
    - Line 43: `cargo run -p alias-daemon -- start &` -> `cargo run -p aliast-daemon -- start &`
    - Lines 52-62: `ALIAS_SUGGESTION_STYLE` -> `ALIAST_SUGGESTION_STYLE` (4 occurrences)
    - Line 61: `ALIAS_SUGGESTION_HIGHLIGHT` -> `ALIAST_SUGGESTION_HIGHLIGHT` (2 occurrences)

    **Update CLAUDE.md:**
    Update user-facing identifiers in CLAUDE.md. The file has GSD-managed sections (marked with GSD comment tags) -- update identifiers within them but do not restructure:
    - Project name references: keep "AliasT" as-is (already correct)
    - Binary name: `alias-daemon` -> `aliast-daemon`
    - Plugin name: `alias.plugin.zsh` -> `aliast.plugin.zsh`
    - Env var names: `ALIAS_*` -> `ALIAST_*` (in stack section tables and text)
    - Crate names: `alias-daemon`, `alias-core`, `alias-protocol` -> `aliast-daemon`, `aliast-core`, `aliast-protocol`
    - Socket path: `alias-$UID/alias.sock` -> `aliast-$UID/aliast.sock`
    - Config directory: `~/.config/alias/` -> `~/.config/aliast/`
    - Internal identifiers: `alias_` -> `aliast_`
    - Rename Scope table already has correct "New" column with aliast names -- but the text references elsewhere need updating

    **Rename GitHub repository (per D-04):**
    Run: `gh repo rename AliasT --yes`
    Then update git remote: `git remote set-url origin https://github.com/cwoo017/AliasT.git`

    Note: Do the repo rename LAST, after all file changes are committed. GitHub auto-redirects old URLs.
  </action>
  <verify>
    <automated>cd /Users/chef/Desktop/projects/Alias && grep -c "alias-daemon\|alias\.plugin\|ALIAS_SUGGESTION\|alias-core\|alias-protocol" docs/terminal-compatibility.md | grep -q "^0$" && echo "DOCS_CLEAN" || echo "STALE_REFS_IN_DOCS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "alias-daemon" docs/terminal-compatibility.md` returns 0
    - `grep -c "alias\.plugin" docs/terminal-compatibility.md` returns 0
    - `grep -c "ALIAS_SUGGESTION" docs/terminal-compatibility.md` returns 0 (only ALIAST_SUGGESTION)
    - `grep "aliast-daemon" docs/terminal-compatibility.md` finds the updated command
    - `grep "aliast.plugin.zsh" docs/terminal-compatibility.md` finds the updated source line
    - `grep -c "alias-daemon" CLAUDE.md` returns 0 (only aliast-daemon)
    - GitHub repo rename confirmed (or noted for manual execution if gh auth fails)
  </acceptance_criteria>
  <done>
    Documentation files updated with all aliast references. GitHub repository renamed from Alias to AliasT with git remote URL updated. All old alias-* references removed from docs.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Full grep audit across ALL project files:
   ```bash
   grep -rn 'alias[-_]daemon\|ALIAS_\|_alias_\|alias\.plugin\|alias\.sock\|alias-nl\|memo=alias[^t]' --include='*.rs' --include='*.zsh' --include='*.toml' --include='*.md' plugin/ docs/ CLAUDE.md
   ```
   Expected: zero hits for old naming (only `aliast` variants). Note: .planning/ files are historical and not checked.

2. Plugin file structure preserved: `wc -l plugin/aliast.plugin.zsh` shows 460+ lines

3. Socket path alignment check:
   - Plugin: `grep "SOCKET_PATH" plugin/aliast.plugin.zsh` shows `aliast-$UID}/aliast/aliast.sock`
   - Daemon: verify in 06-01-SUMMARY.md that lifecycle.rs uses matching path pattern
</verification>

<success_criteria>
- Plugin file renamed to aliast.plugin.zsh (REN-03)
- All _alias_* functions renamed to _aliast_* (REN-05)
- All _ALIAS_* state vars renamed to _ALIAST_* (REN-05)
- All ALIAS_* user env vars renamed to ALIAST_* (shared with REN-04)
- Socket path in plugin matches daemon path (REN-06)
- Documentation updated
- GitHub repo renamed to AliasT (D-04)
- Zero grep hits for old naming in plugin and docs
</success_criteria>

<output>
After completion, create `.planning/phases/06-project-rename/06-02-SUMMARY.md`
</output>
